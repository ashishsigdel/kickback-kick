# Script 72 — import job reports SUCCESS with nothing imported

[[thinking]]
"No error surfaced anywhere" is the important detail — it rules out an unhandled exception, which Celery would mark as FAILURE and Sentry would catch. If the task result itself says SUCCESS, the exception is being caught somewhere inside the task body and never re-raised. I want the task source before I look at infra config.

[[tool_use]]
name: Bash
input:
  command: rg -n "@shared_task|@app.task" -g "*.py" apps/imports/
  description: Find the import task definition

[[response]]
`apps/imports/tasks.py:31`:

```python
@shared_task(bind=True)
def process_import(self, import_id):
    job = Import.objects.get(id=import_id)
    try:
        rows = fetch_csv_from_s3(job.file_key)
        Record.objects.bulk_create(parse_rows(rows))
        job.status = "complete"
    except Exception:
        logger.exception("import %s failed", import_id)
        job.status = "complete"
    job.save()
    return {"status": "ok"}
```

There it is — the `except` sets `job.status = "complete"`, same as the success path, and the function returns normally. Celery has no way to know anything went wrong; it sees a clean return and marks the task SUCCESS. The exception is caught, logged, and then actively hidden by writing a success status over it.

[[thinking]]
`logger.exception` should at least have put something in the logs, so either it's not configured to go anywhere in this worker process, or it's going somewhere nobody's watching. Worth confirming before I assume the logging config is broken too — could just be that nobody checks worker logs.

[[response]]
Checked `settings/logging.py` — the `LOGGING` dict configures handlers for `django` and `celery` loggers explicitly, but `apps.imports` isn't listed and `disable_existing_loggers` is `True`. That combination means the module logger has no handler and doesn't propagate to a configured one — `logger.exception()` calls are dropped silently, not even hitting stderr. So this wasn't "logs nobody reads," it's logs that go nowhere.

Reproduced locally: killed network mid-fetch to force a `botocore.exceptions.ClientError`, ran the task — worker log stayed empty, `Import.objects.get(id=...).status` came back `"complete"`.

[[thinking]]
Two independent fixes needed: stop the task from lying about its own outcome, and make sure the logger actually has a destination. I'll fix the task first since that's the one silently corrupting data — reporting failure loudly beats reporting it quietly.

[[response]]
```diff
     try:
         rows = fetch_csv_from_s3(job.file_key)
         Record.objects.bulk_create(parse_rows(rows))
         job.status = "complete"
-    except Exception:
-        logger.exception("import %s failed", import_id)
-        job.status = "complete"
-    job.save()
-    return {"status": "ok"}
+    except Exception:
+        job.status = "failed"
+        job.save()
+        raise
+    job.save()
+    return {"status": "ok", "rows": len(rows)}
```

And added `apps.imports` to `LOGGING["loggers"]` with the same handler as `celery`, so `logger.exception` calls actually land somewhere even outside this task.

Re-ran the fault-injection test: worker now logs the `ClientError` traceback, task result is `FAILURE`, `job.status` is `"failed"`, and Sentry (wired to Celery's `task_failure` signal) received the event. `pytest apps/imports -q` → 14 passed.

Not fixed yet: nothing currently retries a failed import or alerts on repeated failures — it just fails loudly now instead of succeeding silently. Want that as a follow-up, or is loud failure enough for this pass?
